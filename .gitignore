# Local worktrees
.worktrees/

# Claude Code workspace (local only)
.claude/

# Node
node_modules/

# Packaged extension builds - rebuild with `npm run package`
*.zip

# Chrome extension packing output. NEVER commit the .pem - it is the private
# signing key that owns the extension's ID.
*.pem
*.crx

# Environment / secrets
.env
.env.*
!.env.example

# OS junk
Thumbs.db
ehthumbs.db
desktop.ini
.DS_Store
